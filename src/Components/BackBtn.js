export default function BackBtn() {
    return (
        <div className="mx-16">
            <button className={`text-red border border-red font-poppins bg-white w-full 
            rounded-md mb-3 p-2 hover:bg-red hover:text-white`}>Back</button>
        </div>
    )
}